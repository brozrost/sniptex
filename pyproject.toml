[project]
name = "sniptex"
version = "1.0.0"
description = "..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["requests"]

[tool.setuptools]
packages = ["sniptex"]

[project.scripts]
sniptex = "sniptex.cli:main"